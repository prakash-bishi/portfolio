# Environment variables — copy to .env and fill in real values.
# NEVER commit .env with real values (see SECURITY.md).

# This file is a placeholder until Phase 1 scaffolds the frontend/backend
# projects and real configuration needs are known.

# Example shape (to be replaced with real variables in Phase 1):
# DATABASE_URL=
# DJANGO_SECRET_KEY=
# DJANGO_DEBUG=
# NEXT_PUBLIC_API_BASE_URL=
